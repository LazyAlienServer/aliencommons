services:
  postgres:
    volumes:
      - postgres_data_dev:/var/lib/postgresql
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - "5432:5432"
    env_file:
    - ./env/.env.dev
  
  redis:
    ports:
      - "6379:6379"

volumes:
  postgres_data_dev:
  