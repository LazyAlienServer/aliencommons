{
  "name": "alienmark-service",
  "version": "0.1.0",
  "private": true,
  "description": "Internal HTTP service for AlienMark.",
  "type": "module",
  "main": "./dist/server.mjs",
  "scripts": {
    "build": "vp pack src/server.ts --format esm --dts false",
    "check": "pnpm run lint:check && pnpm run typecheck && pnpm run style:check",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.mjs",
    "lint": "pnpm run lint:check",
    "lint:check": "vp lint src",
    "lint:fix": "vp lint src --fix",
    "style": "pnpm run style:check",
    "style:check": "vp fmt --check package.json src tsconfig.json --ignore-path ../../.prettierignore",
    "style:fix": "vp fmt package.json src tsconfig.json --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "alienmark": "workspace:*",
    "fastify": "^5.6.2"
  }
}
